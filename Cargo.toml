[package]
name = "frinux"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "frinux"
test = false
bench = false

[dependencies]
heapless = "0.9.2"
riscv = { version = "0.16.0", features = ["s-mode"] }
sbi = "0.3.0"
spin = "0.10.0"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
